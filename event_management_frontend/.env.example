# PUBLIC INTERFACE - Environment variables required by the frontend
# Copy this file to .env.local and fill values for local development.

# Backend API base URL (include protocol, no trailing slash), e.g. http://localhost:4000
NEXT_PUBLIC_API_BASE_URL=

# Optional: Title shown in metadata and sidebar
NEXT_PUBLIC_APP_TITLE=EventMaster
